import React from 'react';
import HeroButton from '../hero_button';

const HeroCard = () => {
    return (
        <>
            Card
            <HeroButton />
            <HeroButton />
            <HeroButton />
        </>
    );
};

export default HeroCard;